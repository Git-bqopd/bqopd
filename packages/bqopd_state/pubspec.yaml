name: bqopd_state
description: "State management (BLoCs) for the bqopd platform."
version: 1.0.0
publish_to: 'none'
resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter

  # --- Inter-Package Dependencies ---
  bqopd_models: any
  bqopd_core: any

  # --- External Dependencies ---
  flutter_bloc: ^8.1.5
  equatable: ^2.0.5
  cloud_firestore: ^5.4.3
  firebase_auth: ^5.3.1
  firebase_storage: ^12.3.4