name: bqopd_models
description: "Core data models for the bqopd platform."
version: 1.0.0
publish_to: 'none'
resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  cloud_firestore: ^5.4.3
  equatable: ^2.0.5